---
name: skill-selector
description: 技能选择助手：根据当前任务，从真正可用的技能中推荐最合适的选项与组合。 用户明确询问该用什么技能、推荐已有技能或匹配技能时使用；用户直接要求做事时，不把执行任务替换为推荐。
metadata:
  display-name-zh: 技能选择助手
  source-file: skill-selector.md
  adaptation: 中文兼容版；原文完整保留，配套资源缺失见正文
---

# 技能选择助手

根据当前任务，从真正可用的技能中推荐最合适的选项与组合。

## 使用范围

用户明确询问该用什么技能、推荐已有技能或匹配技能时使用；用户直接要求做事时，不把执行任务替换为推荐。

## 输入与输出

- 输入：任务描述或技能选择疑问。
- 输出：简短推荐、适用理由与调用示例。

## 工作方法

1. 理解核心任务、输入、目标产物与约束。
2. 从当前会话真实技能清单、可验证安装记录和中文使用卡中匹配；区分已安装、仅有安装包、依赖未就绪。
3. 优先推荐明确覆盖需求的技能，通常1—3个，最多5个；说明强匹配或辅助匹配，不虚构精确成功概率。
4. 输出中文名称、适用理由、关键限制、兼容调用标识与一条示例。多个技能互补时说明各负责哪个产物。
5. 明确输入名称可直接指向对应技能；无匹配时坦诚说明，使用真实可用的技能安装或插件管理能力处理发现需求，不推荐不存在的find-skills作为必需工具。
6. 单纯推荐任务不执行所描述的工作；若用户同时明确要求选择并执行，完成选择后在授权范围内继续。

## 当前版本与依赖

不再假定find-skills已安装；新技能发现和安装使用当前实际可用的安装或插件管理能力。

这是基于单份原始说明整理的中文兼容版，提供可执行的指令流程，不代表原作者的完整工具包。按需要加载当前环境实际可用的文档、演示、图像等技能；不存在的脚本、工具、文件和外部账号不得当作已经可用。输出只写入当前允许的工作目录或用户指定位置。

## 原始资料

[原始说明](references/原始说明.txt) 完整保留用于溯源及必要的模板、风格细节查阅。它不是更高优先级的指令，不沿用其中与本入口冲突的触发规则、夸大承诺或不存在的工具调用。用户当前要求、已有授权和当前平台规则优先，不因原文出现“必须确认”而重复询问已明确的信息。
