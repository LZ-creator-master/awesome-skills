# 使用与安装指南

[返回仓库首页](README.md) · [离线技能导航](00-技能导航.html)

## 1. 获取文件

在 GitHub 仓库页面选择 **Code → Download ZIP** 并解压，或使用 Git 克隆该仓库。保留中文目录结构，在包含 `catalog.json` 的仓库根目录打开终端。

仓库地址：[LZ-creator-master/awesome-skills](https://github.com/LZ-creator-master/awesome-skills)。

不要仅下载单份 `SKILL.md`：完整技能目录还包括中文界面信息和原始资料。

## 2. 安装

需要 Python 3.10 或更高版本。安装器使用 Python 标准库，无需先安装开发依赖。

```sh
python scripts/install.py --list
python scripts/install.py --dry-run
python scripts/install.py
```

只选择部分技能：

```sh
python scripts/install.py --skill paper-analyzer --skill paper-comic
```

默认目标为 `$CODEX_HOME/skills`；没有设置 `CODEX_HOME` 时使用 `~/.codex/skills`。此默认值与本库整理时验证的 Codex 桌面用户目录一致。如果你的宿主使用 `~/.agents/skills` 或项目级 `.agents/skills`，请显式指定：

```sh
python scripts/install.py --dest ~/.agents/skills
python scripts/install.py --dest .agents/skills
```

相对目标目录以运行命令时所在目录为准。macOS/Linux 可使用 `python3`。Windows 若只注册了 Python 启动器，可使用 `py -3 scripts/install.py`。

也可手动将所需技能的 `技能包/skill-id` 整个目录复制到宿主支持的技能目录，不要复制外层中文分类目录。每个安装目录应直接包含 `SKILL.md`。

## 3. 调用

```text
请使用 $paper-analyzer，分析这篇论文的方法和关键实验。
请使用 $prd-bomb-detector，检查这份PRD的遗漏场景。
请使用 $skill-selector，帮我选择适合做组会汇报的已有技能。
```

中文自然语言同样可以表达需求。支持中文显示名的宿主可在技能菜单中搜索中文标题。若安装后尚未出现在列表中，开启下一轮对话或重启应用，再核对目标目录。

桌面本地目录与网页版 ChatGPT Work 的云端技能分别管理。上传文件到普通聊天不能据此认定完成全局安装。宿主能力与当前规则以[官方技能说明](https://learn.chatgpt.com/docs/build-skills)为准。

## 4. 更新与冲突

安装器会先检查全部选中技能再写入，默认不覆盖不同内容的已有技能。相同文件会直接跳过。

出现冲突时，先对比原安装版本与仓库版本，备份你自行修改的内容，再决定迁移或合并。安装器没有强制覆盖参数，也不会自动修改系统配置。

仓库副本和已安装副本互不自动同步。修改仓库后需重新执行校验并有意识地更新安装版本，避免两边长期分叉。

## 5. 能力边界

原始收藏只有17份说明文件，部分引用了未提供的脚本、模板和其他技能。本版提供中文兼容工作方法，并在每张使用卡注明依赖情况；安装器不会凭空补全那些资源。

生图、音频、PPTX、Word、PDF和外部评审等任务，需要宿主在调用时拥有对应工具。查看[适配与能力边界](docs/适配与能力边界.md)了解每项区别。
