* text=auto eol=lf
# Keep supplied source bytes intact for provenance hashes.
**/references/原始说明.txt -text
*.png binary
*.zip binary
*.mp4 binary
# Preserve the exact paired-run outputs used for provenance hashes.
evaluation/transformer-pilot/output-*.md -text whitespace=trailing-space,space-before-tab,cr-at-eol
