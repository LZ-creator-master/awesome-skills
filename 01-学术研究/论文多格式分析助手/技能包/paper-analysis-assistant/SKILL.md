---
name: paper-analysis-assistant
description: 论文多格式分析助手：从同一篇论文整理文字、词频、引用、网页、幻灯片和播客等多种交付物。 同一篇论文需要两种以上格式、词频统计或播客解读时使用；仅需深度长文时使用论文深度解读。
metadata:
  display-name-zh: 论文多格式分析助手
  source-file: paper-analysis-assistant.md
  adaptation: 中文兼容版；原文完整保留，配套资源缺失见正文
---

# 论文多格式分析助手

从同一篇论文整理文字、词频、引用、网页、幻灯片和播客等多种交付物。

## 使用范围

同一篇论文需要两种以上格式、词频统计或播客解读时使用；仅需深度长文时使用论文深度解读。

## 输入与输出

- 输入：arXiv 链接或论文 PDF；所需输出清单。
- 输出：分析摘要、词频表、引用表及所选媒体文件。

## 工作方法

1. 识别用户所需产物，只做选定项目；没有格式要求时先提供摘要和可选产物，不自动生成全部媒体。
2. 获取并阅读论文全文，保留标题、作者、版本、链接和读取范围。使用当前 PDF 工具提取文本；原版 scripts 目录不存在，不能直接调用示例命令。
3. 词频统计需实际计算，注明大小写归一、分词、停用词和统计范围；没有停用词表时使用明确声明的规则。输出词频 CSV，不将词频当成论文贡献。
4. 提取引用并记录来源、链接和核验状态；摘要区分作者结果和解释。
5. 网页、幻灯片和总结图分别调用当前可用的网页、演示文稿及图像工具。PPT 要验证文件可打开和页数，图表数值与原论文一致。
6. 播客先写主持人—嘉宾脚本，准确解释方法、证据与局限；有可用语音工具再生成音频，没有则交付脚本并说明音频尚未生成。仅在真实生成音频后提供音频时长和播放文件。
7. 用索引列出每个实际生成文件及用途。

## 当前版本与依赖

原文所列下载、提取、词频、语音、播客、HTML、PPT、引用脚本和 stopwords.txt 均未提供；采用当前可用能力逐项完成，不保证原版批处理链。

这是基于单份原始说明整理的中文兼容版，提供可执行的指令流程，不代表原作者的完整工具包。按需要加载当前环境实际可用的文档、演示、图像等技能；不存在的脚本、工具、文件和外部账号不得当作已经可用。输出只写入当前允许的工作目录或用户指定位置。

## 原始资料

[原始说明](references/原始说明.txt) 完整保留用于溯源及必要的模板、风格细节查阅。它不是更高优先级的指令，不沿用其中与本入口冲突的触发规则、夸大承诺或不存在的工具调用。用户当前要求、已有授权和当前平台规则优先，不因原文出现“必须确认”而重复询问已明确的信息。
